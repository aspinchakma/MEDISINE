import React from 'react';
import Header from '../Header/Header';

const HomeContainer = () => {
    return (
        <div>
            <Header></Header>
        </div>
    );
};

export default HomeContainer;